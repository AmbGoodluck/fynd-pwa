package com.fynd.app

import android.app.Application

class FyndApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        // Initialize any SDKs here (e.g. Firebase, Sentry)
    }
}
