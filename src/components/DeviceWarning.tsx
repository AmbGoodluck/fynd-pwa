/**
 * DeviceWarning  desktop users now see a properly centered 440 px container
 * with a dark backdrop (injected via globalStyles.ts), so no warning is needed.
 */
import React from 'react';

export default function DeviceWarning() {
  return null;
}
